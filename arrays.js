// Arrays en JS

const carrito = ['Producto1', 'Producto2', 'Producot3']

carrito.map( producto => {
    console.log(`El producto es: ${producto}`)
})

const desarrollador = {
    nombre: 'Rocio',
    profesion: 'Desarrolladora Android',
    edad: 23
}

console.log(Object.keys(desarrollador));
console.log(desarrollador);